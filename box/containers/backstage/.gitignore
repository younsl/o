# Dependencies
node_modules/

# Build output
dist/
packages/*/dist/
dist-types/

# Yarn Berry (keep releases, ignore cache and install state)
.yarn/*
!.yarn/releases
!.yarn/plugins
.pnp.*

# =============================================================================
# Credentials & Secrets (HIGH RISK)
# =============================================================================

# Environment files
.env
.env.*
!.env.example
!.env.template

# Local config overrides (may contain real URLs/credentials)
app-config.local.yaml
app-config.production.yaml
*.local.yaml
*.secret.yaml
*.secrets.yaml

# Private keys and certificates
*.pem
*.key
*.p12
*.pfx
*.crt
*.cer
*.der
id_rsa
id_rsa.*
id_ed25519
id_ed25519.*
*.pub

# AWS credentials
.aws/
aws-credentials
credentials.json

# GCP credentials
gcp-*.json
*-service-account.json
service-account*.json
keyfile.json

# Azure credentials
azure-credentials.json
.azure/

# Kubernetes secrets
kubeconfig
kubeconfig.yaml
*.kubeconfig
secrets.yaml
*-secrets.yaml

# Database
*.sqlite
*.sqlite3
*.db

# API keys and tokens (common naming patterns)
*token*.json
*credential*.json
*secret*.json
apikey*
.npmrc
.yarnrc.yml

# Terraform state (may contain secrets)
*.tfstate
*.tfstate.*
.terraform/

# Vault tokens
.vault-token

# =============================================================================
# IDE & Editor
# =============================================================================
.idea/
.vscode/
*.swp
*.swo
*~
.project
.classpath
.settings/

# =============================================================================
# OS Generated
# =============================================================================
.DS_Store
.DS_Store?
._*
Thumbs.db
ehthumbs.db
Desktop.ini

# =============================================================================
# Logs & Debug
# =============================================================================
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*
.npm/
debug/

# =============================================================================
# Testing & Coverage
# =============================================================================
coverage/
.nyc_output/
*.lcov

# =============================================================================
# Temporary files
# =============================================================================
tmp/
temp/
*.tmp
*.temp
*.bak
*.backup
