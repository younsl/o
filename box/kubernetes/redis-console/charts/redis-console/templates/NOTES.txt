Redis Console installed successfully!

Get pod name:
  kubectl get pods -n {{ .Release.Namespace }} -l app.kubernetes.io/instance={{ .Release.Name }}

Connect to console:
  kubectl exec -it -n {{ .Release.Namespace }} $(kubectl get pod -n {{ .Release.Namespace }} -l app.kubernetes.io/instance={{ .Release.Name }} -o jsonpath='{.items[0].metadata.name}') -- redis-console

{{- if .Values.config.clusters }}
Configured clusters: {{ len .Values.config.clusters }}
{{- else }}
⚠️ No clusters configured. Add to values.yaml:
  config:
    clusters:
      - alias: production
        host: redis-prod.example.com
        port: 6379
{{- end }}
