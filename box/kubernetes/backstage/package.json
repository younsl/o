{
  "name": "backstage-gitlab-discovery",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "predev": "node scripts/generate-build-info.js",
    "dev": "concurrently \"yarn start\" \"yarn start-backend\"",
    "start": "yarn workspace app start",
    "start-backend": "yarn workspace backend start",
    "build:backend": "yarn workspace backend build",
    "build:all": "node scripts/generate-build-info.js && backstage-cli repo build --all",
    "tsc": "tsc --build",
    "clean": "backstage-cli repo clean"
  },
  "workspaces": {
    "packages": [
      "packages/*",
      "plugins/*"
    ]
  },
  "devDependencies": {
    "@backstage/cli": "^0.36.1",
    "concurrently": "^8.2.2",
    "jest-environment-jsdom": "^29.7.0",
    "typescript": "~5.4.0"
  },
  "resolutions": {
    "@types/react": "^18",
    "@types/react-dom": "^18",
    "@backstage/backend-app-api": "^1.6.1",
    "@backstage/backend-defaults": "^0.17.0"
  },
  "packageManager": "yarn@4.12.0"
}
